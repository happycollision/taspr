{
  "name": "taspr",
  "version": "0.1.0",
  "description": "CLI tool for managing stacked PRs on GitHub",
  "type": "module",
  "bin": {
    "taspr": "./dist/taspr"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "build": "bun build src/cli/index.ts --compile --outfile dist/taspr",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
